name: on_push
on:
  push:
    branch:
jobs:

  build_and_test:
    runs-on: ubuntu-latest
    env:
      RUNNER_OS: true
      HEADLESS_CHROME: true
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2

        with:
          distribution: 'zulu'
          java-version: 17
      - name: Run tests with maven
        run: mvn test

          # Handle remote server chromedriver-linux
      # What steps within each job